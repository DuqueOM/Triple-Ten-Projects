paths:
  weather_data: moved_project_sql_result_07.csv
  processed_dir: data/processed
  models_dir: models

training:
  test_size: 0.2
  validation_size: 0.16

model:
  type: random_forest
  params:
    n_estimators: 200
    max_depth: 6
    min_samples_leaf: 5

logging:
  level: INFO
